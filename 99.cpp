#include <bits/stdc++.h>

using namespace std;
int main(){
  int n;
  cin >> n;
  if (n <= 99){
    cout << "99\n";
  }
  // 300 200
  // else if (abs(n-ceil(n)) < abs(n-floor(n))){
    // cout << ceil(n)-1;
  // }
  // cout << ceil(1000) << floor(1000);
}
